import {t} from "../i18n/translater.js";

export const layout = {
    name: 'layout',
    label: t.fields.layout,
    widget: 'select',
    options: ['blocks'],
    required: false,
    i18n: 'duplicate'
}